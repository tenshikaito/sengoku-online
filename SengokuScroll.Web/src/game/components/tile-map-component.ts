import TileMapComponentBase from "./tile-map-component-base";

export default class TileMapComponent extends TileMapComponentBase {

    public constructor(scene: Phaser.Scene) {

        super(scene);
    }
}